
services:
  categories-service:
    container_name: categories-service
    build: .
    ports:
      - "3010:3010"
    env_file:
      - ./.env
    networks:
      - support-network

networks:
  support-network:
    external: true
